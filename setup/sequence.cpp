#include <vector>
#include "number.h"
#include "sequence.h"

bool Sequence::detect(std::vector<Number> &nums,std::vector<std::string> dlls){
    return bool();
}

Number Sequence::term(long long int n){
    return Number();
}